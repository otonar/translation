# CLAUDE.md

## 触らないファイル

以下のファイルは認証情報・個人データを含むため、読み取り・編集・削除・内容の表示を一切行わないこと。

| ファイル | 理由 |
|---------|------|
| `.env` / `.env.*` | APIキーなどの認証情報 |
| `api_key.txt` | APIキー |
| `*.pem` / `*.key` / `*.p12` | 証明書・秘密鍵 |
| `logs/*.txt` | ユーザーの翻訳履歴（会話内容を含む） |

これらのファイルが話題に上がった場合も、内容を読んだり出力したりしないこと。
